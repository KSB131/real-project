package com.quadcore.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import com.quadcore.domain.Member;

public interface MemberRepository extends JpaRepository<Member, Long> {
}