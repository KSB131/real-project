package com.carepose.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import com.carepose.entity.Member;

@repository
public interface MemberRepository extends JpaRepository<Member, Long> {
}